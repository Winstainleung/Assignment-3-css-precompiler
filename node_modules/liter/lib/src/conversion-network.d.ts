import { Unit } from './unit';
export declare class UnitConversionNetwork {
    private edges;
    private addEdge(left, leftRate, right, rightRate);
    addRate(num: number, numUnit: Unit, denom: number, denomUnit: Unit): void;
    private rates?;
    private computeRates();
    getConversionUnit(from: Unit, to: Unit): Unit | undefined;
}
